version: '3.8'

services:
  web:
    image: placeholder:latest  # Se actualizará automáticamente en el despliegue
    ports:
      - "8080:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production